# Virtual environment
.venv/
venv/
ENV/

# macOS system files
.DS_Store
.AppleDouble
.LSOverride

# Environment variables / Secrets
.env
*.env

# Python build/test artifacts
__pycache__/
*.pyc
.pytest_cache/

# Runtime storage (uploaded datasets, analysis outputs)
data/
# Large reference PDFs (kept locally, not in git — see docs/references/*.txt for extracted text)
docs/references/*.pdf
